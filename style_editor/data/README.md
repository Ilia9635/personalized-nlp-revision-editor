# Data

This folder is reserved for local project data.

The full author-specific writing datasets are private and are not included in this repository. Local CSV and JSONL files should be placed in `data/raw/` and `data/processed/` during development.